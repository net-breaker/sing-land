{
  "name": "singland",
  "version": "1.0.0",
  "description": "A GUI for sing-box",
  "homepage": "https://github.com/net-breaker/sing-land",
  "repository": "https://github.com/net-breaker/sing-land",
  "author": "anonysoul <i@anonysoul.com>",
  "main": "main.js",
  "scripts": {
    "init": "wireit",
    "start": "wireit",
    "build": "wireit"
  },
  "wireit": {
    "init": {
      "command": "cp ../controller/*.js ./ && cp ../editor/*.js ./",
      "dependencies": [
        "../controller:tsc",
        "../editor:tsc"
      ]
    },
    "start": {
      "command": "electron .",
      "dependencies": [
        "init"
      ]
    },
    "build": {
      "command": "electron-builder build --publish=never",
      "dependencies": [
        "init"
      ]
    }
  },
  "dependencies": {
    "electron-context-menu": "^3.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.3",
    "electron": "^23.1.3",
    "electron-builder": "^23.6.0",
    "typescript": "^5.0.2",
    "wireit": "^0.9.5"
  }
}
